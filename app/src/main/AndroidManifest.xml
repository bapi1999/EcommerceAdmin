<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecommerce_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecommerce">
        <activity
            android:name=".activities.EditProductActivity"
            android:exported="true" />
        <activity
            android:name=".activities.OtpVerificationActivity"
            android:exported="true" />
        <activity
            android:name=".activities.TakingDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.WithdrawalActivity"
            android:exported="true" />
        <activity android:name=".activities.PriceCalculatorActivity" />
        <activity android:name=".activities.MyAccountActivity" />
        <activity android:name=".activities.ProfileMenuActivity" />
        <activity android:name=".activities.OrderDeatilsActivity" />
        <activity android:name=".activities.AddProductActivity" />
        <activity android:name=".activities.ProductDetailActivity" />
        <activity
            android:name=".activities.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Ecommerce.NoActionBar" />
        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>